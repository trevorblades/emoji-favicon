import future from '@mdx-deck/themes/future';
import highlight from '@mdx-deck/themes/syntax-highlighter';

export const themes = [
  future,
  highlight
];

# Slide decks with style

Using an emoji favicon with Gatsby themes and MDX Deck

---

1. Add `gatsby-theme-mdx-deck` to your Gatsby config
2. Configure `gatsby-plugin-emoji-favicon` in your Gatsby config
3. Create some slides in `decks/index.mdx`

---

```js
// gatsby-config.js
module.exports = {
  plugins: [
    'gatsby-theme-mdx-deck',
    {
      resolve: 'gatsby-plugin-emoji-favicon',
      options: {
        emoji: '🛶'
      }
    }
  ]
};
```

---

Read more and view the source code for this example [on GitHub](https://github.com/trevorblades/emoji-favicon/tree/master/example).
